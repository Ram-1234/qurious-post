generator client {
    provider="prisma-client-js"
}

datasource db {
    provider = "mongodb"
    url = env("DATABASE_URL")
}

// model User {
//     id String @id  @default(auto()) @map("_id") @db.ObjectId 
//     email String @unique
//     username String @unique 
//     password String
//     avatar String?
//     createAt DateTime @default(now()) 
// }


model User {
  id String @id @default(auto()) @map("_id") @db.ObjectId
  email String @unique 
  username String @unique
  password String // Adjust as needed
  avatar String? // Store URL or file path
  createdAt DateTime @default(now())
}
